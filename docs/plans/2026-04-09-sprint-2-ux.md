# Sprint 2 Implementation Plan: UX & Trust

**Goal:** Improve the initial teacher experience through onboarding and real dashboard metrics, while boosting landing page conversion with social proof.

---

### Task 1: Onboarding System (Backend)

**Files:**
- `backend/apps/auth/models.py`
- `backend/apps/auth/router.py`
- `backend/fix_db.py`

**Steps:**
1. Add `onboarding_completed = Column(Boolean, default=False)` to `User` model.
2. Update `fix_db.py` to add `onboarding_completed` (Boolean) to `users` table.
3. Add `POST /auth/onboarding-complete` endpoint.

---

### Task 2: Onboarding System (Frontend)

**Files:**
- `front/src/components/Onboarding/OnboardingModal.tsx` (New)
- `front/src/context/AuthContext.tsx`
- `front/src/pages/dashboard/TeacherDashboard.tsx`

**Steps:**
1. Create a sleek 4-step onboarding modal.
2. Logic in `TeacherDashboard` to show modal if `user.onboarding_completed` is false.
3. Update `AuthContext` user state after completion API call.

---

### Task 3: Teacher Stats API & Dashboard

**Files:**
- `backend/apps/generator/router.py`
- `front/src/pages/dashboard/TeacherDashboard.tsx`

**Steps:**
1. Implement `GET /stats/me` with real counters from `generation_logs`.
2. Add `recharts` for the activity graph.
3. Refactor Dashboard UI into a clean, modern grid.

---

### Task 4: Landing Page Polish

**Files:**
- `front/src/pages/Landing.tsx`
- `front/index.html`

**Steps:**
1. Implement "Testimonials" section.
2. Implement "FAQ" accordion.
3. Add SEO & OG meta tags.

---

## Definition of Done
- [ ] Onboarding works for new users.
- [ ] Dashboard shows real data.
- [ ] Landing page has Reviews and FAQ.
- [ ] No regressions in B2B logic.
